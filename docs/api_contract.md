# API Contract

Primary workflow endpoint: `POST /claims/review`.

Operational endpoints: `GET /agents`, `GET /approvals/pending`, approval action endpoints, `GET /audit-logs`, `GET /policies`, and `POST /policies/evaluate`.

