# Architecture

The platform routes insurance claim agent recommendations through a central governance gateway. Agents produce facts, policy evidence, fraud indicators, and recommendations. The gateway applies deterministic prompt-injection detection, policy rules, and risk scoring before any downstream action can proceed.

